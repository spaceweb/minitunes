%h3 Similar Artists
- if @artist.artist_similars.length > 0
  - @artist.artist_similars.each_with_index do |artist, i|
    - break if i == 20
    %pre
      = link_to artist.similar_name, artist_path(uri(artist.similar_name))
